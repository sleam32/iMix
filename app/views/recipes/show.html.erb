<header class="navbar navbar-fixed-top">
  <ul class="nav">
    <li class="active">
      <li><%= link_to "Home", :root %></li>
    </li>
    <li><a href="#">About iMix</a></li>
    <li><a href="#">Blog</a></li>
    <li><a href="#">Contact</a></li>
  </ul>
  <nav class="navbar-inner">
    <div class="container">
      <div id="search_bar">
        <%= form_tag new_recipe_path, :method => 'get' do %>
        <p>
          <%= text_field_tag :search, params[:search] %>
          <%= submit_tag "Search", :name => nil %>
        </p>
        <% end %>
      </div>
    </header>
  </nav>
</div>

<div class="almost-done">
  <h2>Almost Done!</h2>
</div>

<div id="magic">
  <p>1. Select your ingredients...</p>
  <p>2. Hand this off to your bartender...wait...</p>
  <p>3. Taste the magic</p>
</div>


<div id="list-info">
  <ul>
    <li>Drink Name: <%= @recipe.drink_name %></li>
    <li>Your Name: <%= @recipe.user_name %></li>
    <li>Your State: <%= @recipe.state %></li>
  </ul>
</div>

<div id="dropdown">
  <%= form_for @recipe do |f| %>
  <%= f.collection_select(:ingredients, @allIngredients, :id, :name ) %>
  <%= f.submit "Submit" %>
  <% end %>
</div>

<div id="ingredients">
  <ul>
    <% @recipe.ingredients.each do |ingredient| %>
    <li><%= ingredient.name %> <%= link_to 'x', :method => :destroy %></li>
    <% end %>
  </ul>
</div>

<div id="save-link">
  <%= button_to "Save", :root %>
</div>

<!-- <div class="slogan2">
  <p>"because alcohol SUCKS...said noone...EVER!"</p>
</div> -->


